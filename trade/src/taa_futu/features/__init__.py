"""JQ Quant 的功能插件。

这个包里每个模块登记一个功能。核心（``taa_futu.plugin``）不 import 它们，
而是启动时扫描本包、让各模块自行登记——所以新增功能只要往这里丢一个文件，
不需要改核心，也不需要改导航。

每个模块应当很薄：描述这个功能是什么，然后把已有的 ``render_x(settings)``
挂上去。真正的实现留在原来的位置，避免为了插件化而搬运大段代码。
"""
